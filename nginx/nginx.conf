upstream django {
    server web:8000;
}

# Redirect HTTP to HTTPS - uncomment after SSL is set up
# server {
#     listen 80;
#     server_name easytrackmanager.com www.easytrackmanager.com;
#     return 301 https://$host$request_uri;
# }

server {
    listen 80;
    server_name easytrackmanager.com www.easytrackmanager.com;
    client_max_body_size 10M;

    location /static/ {
        alias /app/staticfiles/;
    }

    location /media/ {
        alias /app/media/;
    }

    location / {
        proxy_pass http://django;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 120s;
        proxy_send_timeout 120s;
        proxy_read_timeout 120s;
    }
}

# HTTPS - uncomment after certbot and add volume: - /etc/letsencrypt:/etc/letsencrypt:ro
# server {
#     listen 443 ssl;
#     server_name easytrackmanager.com www.easytrackmanager.com;
#     client_max_body_size 10M;
#     ssl_certificate /etc/letsencrypt/live/easytrackmanager.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/easytrackmanager.com/privkey.pem;
#     location /static/ { alias /app/staticfiles/; }
#     location /media/ { alias /app/media/; }
#     location / {
#         proxy_pass http://django;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto https;
#         proxy_connect_timeout 120s; proxy_send_timeout 120s; proxy_read_timeout 120s;
#     }
# }
